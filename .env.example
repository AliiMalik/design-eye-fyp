# Root-level env consumed by docker-compose.yml.
# Copy to .env and set a real secret:  openssl rand -base64 48
JWT_SECRET=replace-with-a-random-48-byte-secret

LLM_PROVIDER=mock
LLM_MODEL=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
